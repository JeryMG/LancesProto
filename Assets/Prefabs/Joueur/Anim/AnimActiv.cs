using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimActiv : MonoBehaviour
{
    /*[SerializeField] private Animator animbody;
    public  List<AnimatorController> Anim=new List<AnimatorController>();
    private float TotatFrame;
    private bool toucher=false;
    [SerializeField] private int i = 0;

    void Update()
    {
        if(Input.GetKeyDown(KeyCode.P))

       // if (transform.hasChanged==true)
        {
            //if (toucher == false)
            //{
            //    animbody.runtimeAnimatorController = Anim[0];
                
            //}
            if (i < Anim.Count - 1)
            {
                i++;
            }
            else
            {
                i = 0;
            }
            animbody.runtimeAnimatorController = Anim[i];


            //animbody.runtimeAnimatorController = (RuntimeAnimatorController)Resources.Load("Assets/Prefabs/Joueur/Anim/Course.controller");

        }
      
    }*/
}
